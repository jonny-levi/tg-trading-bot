---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: money-bot
  namespace: money-bot
  labels:
    app: money-bot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: money-bot
  template:
    metadata:
      labels:
        app: money-bot
    spec:
      containers:
        - name: money-bot
          image: 172.20.10.103:5000/moneyboy:v1.0.0
          ports:
            - containerPort: 32121
