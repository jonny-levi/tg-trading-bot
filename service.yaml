apiVersion: v1
kind: Service
metadata:
  name: money-bot-svc
  namespace: money-bot
spec:
  type: LoadBalancer
  selector:
    app: money-bot
  ports:
    - port: 5000
      targetPort: 5000
      # nodePort: 31212
